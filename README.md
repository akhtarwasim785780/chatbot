# chatbot
A simple Python terminal-based chat app using OpenAI’s GPT-3.5 API. It stores conversation history, sends user queries to the model, and prints AI replies. Includes error handling, a chat loop, and a custom system prompt for a concise assistant persona named Gemini. Ideal for learning APIs and building AI chatbots.
